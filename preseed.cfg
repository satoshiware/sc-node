# =====================================================================
# Custom preseed.cfg for Debian 13 "Trixie" – Headless Minimal Install
#
# This file is based directly on the official Debian example-preseed.txt,
# with only the following intentional changes:
#
#   - Uses a generic UTC timezone (location-independent)
#   - Sets hostname to 'sc-node' and domain to 'internal'
#   - Automatically creates user 'satoshi' (Name: 'Satoshi Nakamoto', passwd: 'satoshi') w/ sudo rights
#   - Installs curl, sudo, and openssh-server utilities
#   - Performs fully automatic LVM partitioning (erases the first disk found)
#   - Root login is disabled
#   - Copies needed scripts (late-commands.sh and firstboot.sh) from /cdrom/sc-node to target system (/root/setup)
#   - Runs /root/setup/late-commands.sh, which configures the system to execute firstboot.sh automatically on first boot
#
# Debugging late commands: grep preseed-late /var/log/installer/syslog
#   - Note: syslog disappears on first boot
# =====================================================================

### Make extra sure everything is wiped from the install disk!
d-i partman/early_command string \
    DISK=$(list-devices disk | head -n1) ; \
    logger -t preseed-early "early_command started - detected disk: $DISK" ; \
    dd if=/dev/zero of=$DISK bs=1M count=500 status=none || logger -t preseed-early "First dd failed on $DISK" ; \
    dd if=/dev/zero of=$DISK bs=512 seek=$(($(blockdev --getsz $DISK) - 2048)) count=2048 status=none || logger -t preseed-early "Second dd failed on $DISK" ; \
    sync ; \
    logger -t preseed-early "early_command finished - setting disk: $DISK" ; \
    debconf-set partman-auto/disk "$DISK" || logger -t preseed-early "debconf-set failed for $DISK"

### Localization
d-i debian-installer/locale string en_US.UTF-8
d-i keyboard-configuration/xkb-keymap select us

### Network
d-i netcfg/choose_interface select auto
d-i netcfg/get_hostname string sc-node
d-i netcfg/get_domain string internal
d-i netcfg/wireless_wep string

### Mirror
d-i mirror/country string manual
d-i mirror/http/hostname string deb.debian.org
d-i mirror/http/directory string /debian
d-i mirror/http/proxy string

### Clock / Timezone
d-i clock-setup/utc boolean true
d-i time/zone string Etc/UTC
d-i clock-setup/ntp boolean true

### Partitioning – Automatic LVM
d-i partman-auto/method string lvm
d-i partman-auto-lvm/guided_size string max
d-i partman-lvm/device_remove_lvm boolean true
d-i partman-md/device_remove_md boolean true
d-i partman-lvm/confirm boolean true
d-i partman-lvm/confirm_nooverwrite boolean true
d-i partman-auto/choose_recipe select atomic

# Confirmations (harmless redundancy)
d-i partman-partitioning/confirm_write_new_label boolean true
d-i partman/choose_partition select finish
d-i partman/confirm boolean true
d-i partman/confirm_nooverwrite boolean true

### Force UEFI/GPT
d-i partman-efi/non_efi_system boolean true

### Clean UEFI/GPT installs (forces GPT label)
d-i partman-partitioning/choose_label select gpt
d-i partman-partitioning/default_label string gpt

### Apt setup – Enable network mirror + skip prompts
d-i apt-setup/use_mirror boolean true
d-i apt-setup/cdrom/set-first boolean false

### Disable popularity contest survey (no prompt)
popularity-contest popularity-contest/participate boolean false

### Boot loader installation
d-i grub-installer/only_debian boolean true
d-i grub-installer/with_other_os boolean false

### Finishing up the installation
d-i finish-install/reboot_in_progress note

### Account setup
d-i passwd/root-login boolean false
d-i passwd/make-user boolean true
d-i passwd/user-fullname string Satoshi Nakamoto
d-i passwd/username string satoshi
d-i passwd/user-uid string 1000
d-i user-setup/allow-password-weak boolean true
d-i passwd/user-password password satoshi
d-i passwd/user-password-again password satoshi
d-i passwd/user-default-groups string sudo

### Packages
tasksel tasksel/first multiselect standard
d-i pkgsel/include string curl sudo openssh-server
d-i pkgsel/install-language-support boolean false
	
### Custom late commands
# Copies late-commands.sh and firstboot.sh from /cdrom/sc-node/ into /root/setup
# Makes those files executable and runs late-commands.sh immediately
# Note: late-commands.sh sets up firstboot.sh to run once on first boot (via systemd oneshot)
# Debugging: grep preseed-late /var/log/installer/syslog
#     Note: syslog disappears on first boot
d-i preseed/late_command string \
    logger -t preseed-late "Starting late commands..." ; \
    mkdir -p /target/root/setup ; \
    cp -a /cdrom/sc-node/late-commands.sh /target/root/setup/ || logger -t preseed-late "Copy late-commands.sh failed" ; \
    cp -a /cdrom/sc-node/firstboot.sh /target/root/setup/ || logger -t preseed-late "Copy firstboot.sh failed" ; \
    in-target chown -R root:root /root/setup || logger -t preseed-late "chown failed" ; \
    in-target chmod -R u=rwx,go=rX /root/setup || logger -t preseed-late "/root/setup chmod failed" ; \
    in-target chmod +x '/root/setup/*.sh' || logger -t preseed-late "chmod +x on scripts failed" ; \
    in-target /root/setup/late-commands.sh || logger -t preseed-late "late-commands.sh failed" ; \
    logger -t preseed-late "Late commands completed"