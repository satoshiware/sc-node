# Satoshiware Dashboard (React + Tailwind)

Quick scaffold to run a Coinbase-style dashboard UI using Vite, React and Tailwind.

Install and run:

```bash
npm install
npm run dev
```

This creates a minimal dashboard layout with placeholder chart, order book, trade history, buy panel, and orders table.
